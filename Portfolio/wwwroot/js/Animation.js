/*window.onload = callRepeater;

function callRepeater() {
    let element = document.getElementById('test');
    repeat(200, 0, 100, 1, element);
}

function repeat(time, start, end, step, element) {
    for (let i = start; i < end; i++) {
        element.innerHTML = i;
        sleep(time);
    }
}*/